# Seguimiento del proyecto — Especificación de Proyecto Final (Leodega)

> Documento de control interno (no forma parte del entregable). Registra qué se ha hecho, qué decisiones se tomaron y por qué, y qué falta, para poder retomar el trabajo sin perder contexto.

**Curso:** SOFG1007 — Ingeniería de Software I (ESPOL/FIEC)
**Proyecto real:** Leodega (marketplace de almacenamiento), continuación de un sistema entregado por un equipo anterior.
**Fuente de la plantilla:** `project_instructions.md`.

---

## 1. Estado por sección

| Sección | Estado | Notas |
|---|---|---|
| Portada | 🟢 Completa | Matrícula de los 4 integrantes confirmada |
| Acta de Conformidad del Cliente | 🟢 Completa | Las dos actas resueltas: `imagenes/acta_conformidad.pdf` (cuerpo principal, ver §2 punto 35) y `imagenes/acta_conformidad_ers.pdf` (Anexo, ver §2 punto 34) |
| Índices (TOC/LOF/LOT) | 🟢 Automáticos | No requieren edición manual |
| Información General del Proyecto | 🟢 Completa | Descripción, objetivos y alcance, tomados de `proyecto1P.tex` |
| Sección A — Gestión de Riesgos | 🟢 Completa | Matriz 5×5 + 9 riesgos evaluados y con plan de respuesta |
| Sección A — Sprint Backlogs | 🟢 Completa | 5 sprints, 38 historias reales | Ver convenciones (§3) sobre "Responsable" y fechas |
| Sección A — Cronograma | 🟢 Completa | Tabla CPM, ruta crítica, AOA y recursos limitados (imágenes reales del usuario) + Gantt generado por Claude |
| Sección B — Casos de uso | 🟢 Completa | 6 diagramas PlantUML reales (1 general de contexto + 5 por subsistema), tabla de actores (8 actores), catálogo consolidado de 43 UC y 11 fichas extendidas de los casos medulares |
| Sección B — Diagrama de clases (SOLID/patrones/malos olores) | 🟢 Completa | Dividido en 10 diagramas PlantUML (1 vista general por paquetes + 9 de detalle, uno por paquete) para legibilidad sin zoom (ver §2 punto 26); 19 modelos + 8 servicios + 3 policies + enum + 3 excepciones + 6 clases nuevas planificadas; tablas de SOLID, patrones y code smells, trazabilidad por diagrama |
| Sección B — Objetos | 🟢 Completa | 2 diagramas de objetos reales (reserva confirmada con pago; reporte de incidente), valores coherentes con el diagrama de clases |
| Sección B — Componentes y Despliegue | 🟢 Completa | PlantUML real (recursos/diagramas_uml/), sistema completo heredado+nuevo marcado 🟦/🟩, con tabla de trazabilidad |
| Sección C — Actividad | 🟢 Completa | 12 diagramas PlantUML reales, cada uno con descripción y trazabilidad — pulled en commit `fe7cf29` ("Diagramas de actividad", Carla/Geovanny) |
| Sección C — Secuencia (32) | 🟢 Completa | 32 diagramas PlantUML reales (20 heredados + 12 nuevos planificados), cada uno con descripción y trazabilidad — commit `85994b2` |
| Sección C — Colaboración | 🟢 Completa | 6 diagramas de comunicación PlantUML reales, cada uno con numeración jerárquica de mensajes y trazabilidad — ver §2 punto 29 |
| Sección C — Estado | 🟢 Completa | 5 diagramas reales (`Reservations`, `StoreRooms`, `Reports`, `Payments`, `User`), cada uno con tabla de trazabilidad; documentan además 2 hallazgos reales de inconsistencia en el código (ver §2, punto 14) |
| Contribuciones Individuales | 🟢 Completa | Nombres, contribución específica y % de aporte (25\% cada uno, ver §2 punto 25) de los 4 integrantes |
| Referencias | 🟢 Completa | Sin placeholders |
| Anexos | 🟢 Completa | ERS mejorado (Volere, 38 HU + 11 RNF detallados), flujo de ventanas (PlantUML) + 34 capturas reales del prototipo por rol, y acta de conformidad del anexo — ver §2 punto 32 |

Leyenda: 🟢 completo · 🟡 parcial/falta un dato puntual · 🔴 pendiente (trabajo sustancial)

---

## 2. Qué se hizo, en orden

1. **Plantilla LaTeX inicial** generada a partir de `project_instructions.md`: estructura completa (`main.tex` + `secciones/*.tex`), con placeholders en todo lo que es específico del proyecto. Logo ESPOL (no ETS). Corregidos dos bugs de compilación (guion bajo fuera de modo matemático → paquete `underscore`; `\\` seguido de `[` interpretado como argumento óptico → se separó con `\\{}`).
2. **Información General** (Descripción general, Objetivos, Alcance) completada con datos reales de `recursos/informacion_general/proyecto1P.tex` (el ERS del primer parcial).
3. **Gestión de Riesgos** completada con `recursos/riesgos/analisis_de_riesgo.md` (metodología, matriz 5×5, 6 estrategias) y `Estimación de Esfuerzo con COSMIC - Leodega.xlsx` (para justificar R-01/R-02 por concentración de tamaño funcional). 9 riesgos identificados y evaluados (R-01 a R-09), cada uno con estrategia, acciones de mitigación, plan de contingencia y responsable.
4. **Placeholders de portada** llenados: logo real (`imagenes/logo-espol.jpg`), nombre del sistema (Leodega), integrantes del equipo, profesor, cliente ("Leonardo", único nombre disponible en las actas de reunión) y período académico (2026 PAO2).
5. **Preparación del repositorio git** para `https://github.com/Darwin4050E/DocumentacionLeodega.git`: `git init`, `.gitignore` (excluye artefactos de compilación LaTeX y `.claude/settings.local.json`), commit `7d6684c` sobre el `README.md` ya existente en el remoto. **Aún no se ha hecho `git push`** (pendiente de confirmación del usuario, y hay cambios nuevos sin commitear desde entonces — ver `git status`).
6. **Corrección de tablas desbordadas**: la matriz de riesgo (Tabla 2.1) se ajustó con `\resizebox`; la tabla de plan de respuesta (Tabla 2.3) tenía el header "Responsable" desbordando su celda, se corrigió ensanchando la columna y reduciendo `\tabcolsep`.
7. **Sprint Backlogs completados** (5 sprints, 38 historias de usuario reales HUG/HUC/HUA/HUE/HUL, tomadas del capítulo "User Stories" de `proyecto1P.tex`, agrupadas por el campo "Sprint" que ya traía cada historia). Ver convenciones adoptadas en §3.
8. **Centrado de tablas**: se añadieron los tipos de columna reutilizables `M{ancho}` (centrado horizontal+vertical) y `L{ancho}` (alineado a la izquierda, centrado vertical) en `main.tex`, aplicados a **todas** las tablas del documento (Sección A, Sección B, portada, contribuciones) para que headers y contenido dejen de quedar pegados arriba.
9. **Cronograma del Proyecto completado**: tabla de 38 actividades (HST-001 a HST-038) con dependencias, duración, personal, EET/holgura/LET y marca de ruta crítica; texto de ruta crítica (52 días); sección nueva de planificación con recursos limitados (87→85 días, límite 4 personas). Todo tomado del capítulo "Project Planning and Control" de `proyecto1P.tex`.
10. **Imágenes del cronograma insertadas**: el usuario proporcionó las imágenes reales de AOA (`imagenes/activity-on-arrow.png`) y de recursos limitados sin optimizar/optimizado (`imagenes/resoruce-limited-without-optimizatio.png` y `imagenes/resource-limited-optimization.png` — nombres de archivo tal cual los dejó el usuario, con una errata en el primero que no se corrigió porque hay que respetar el nombre real del archivo en disco). No tenía el diagrama de Gantt, así que Claude lo generó programáticamente con Python/Matplotlib (`imagenes/diagrama_gantt.png`) a partir de los mismos datos EET/duración/ruta crítica de la tabla CPM, con la misma paleta de colores por sprint que las otras imágenes para mantener consistencia visual. Se documentó en el propio LaTeX que la herramienta utilizada fue "Python/Matplotlib" en vez de dejar el placeholder de herramienta comercial.
11. **Diagramas de Componentes y Despliegue generados** (primeros diagramas UML reales del entregable, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`): se exploró el código de `recursos/leodega_codigo/` (capas Controller→Service→Model, middleware, config de filesystems/database/CORS/sanctum) y la infraestructura real de despliegue (`railway.json`, `Procfile`, `DockerFIle`, `vercel.json`, `docker-compose.yml`, `trustHosts` en `bootstrap/app.php`), y se cruzó contra el ERS (`proyecto1P.tex`, sección "Project Context"/"Project Scope") para separar heredado 🟦 de nuevo 🟩. Confirmado por grep que ninguno de los requerimientos nuevos (organizaciones, Leodeguita, integración de inventario, permiso de bomberos, panel admin/métricas, moderación de cuentas con auditoría) tiene código todavía — solo existe el campo `User.state` como base parcial de moderación; por eso esos módulos se marcan "Nuevo (planificado)" con la sección del ERS como fuente, tal como indica la sección 4 de `INSTRUCCIONES_DIAGRAMAS.md` para elementos nuevos sin código. Fuentes PlantUML en `recursos/diagramas_uml/*.puml`, renderizadas a PNG localmente hacia `imagenes/diagrama_componentes.png` y `imagenes/diagrama_despliegue.png`. Se evitó el placeholder genérico de herramienta del macro `\DiagramaPagina` (igual que se hizo con el Gantt): estas dos secciones usan bloques manuales en `seccionB.tex` con la herramienta real ("PlantUML") y la tabla de trazabilidad obligatoria. Se agregaron los colores `heredadobg`/`nuevobg` en `main.tex` (mismos tonos que el `skinparam` de los `.puml`) para colorear la columna "Estado" de las tablas. Se compiló el documento completo sin errores; se corrigieron overfull hbox usando `\url{}` en vez de `\texttt{}` para rutas de archivo largas (permite salto de línea en `/`) y reestructurando la oración del despliegue que encadenaba demasiados tokens verbatim seguidos.
12. **Actualización de herramienta PlantUML (2026-08-12)**: el usuario instaló PlantUML v1.2026.6 en `~/.local/share/plantuml/plantuml.jar` junto con Graphviz (`dot` 2.43.0), que antes faltaba y había forzado el uso del motor de layout alternativo Smetana (más tosco). Se quitó el `!pragma layout smetana` de ambos `.puml`, se regeneraron los PNG con el motor nativo `dot` (notoriamente más limpio: usa los íconos UML de componente correctos y mejor distribución) y se actualizó el texto "Herramienta de modelado utilizada" en `seccionB.tex` para citar la versión real y el motor Graphviz/`dot`. Recompilado sin errores nuevos.
13. **Diagrama de Clases, Objetos y Estado generados (2026-08-13)**, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`: se exploró a fondo `recursos/leodega_codigo/backend` (19 modelos Eloquent, 6 servicios de aplicación, 2 policies, 1 enum, 2 excepciones de dominio, y las migraciones para extraer atributos/tipos/enums exactos y las reglas `onDelete(cascade)` que fundamentan cada composición vs. asociación del diagrama de clases). El **Diagrama de Clases** (`recursos/diagramas_uml/diagrama_clases.puml`) modela toda la capa de lógica de negocio heredada más 6 clases nuevas planificadas (`Organizacion`, `OrganizacionMiembro`, `ConexionInventario`, `ProductoInventario`, `PermisoBomberos`, `RegistroAuditoria`) extraídas del ERS (`proyecto1P.tex`, HUE-01..06, HUC-10..13, HUA-01..03); se documenta además el patrón real de "perfil compuesto" (`User` 1–(0..1) `Landlords`/`Tenants`/`Admin` vía `hasOne`, no herencia). Se llenaron las tablas de SOLID y Patrones de Diseño que ya traía la plantilla (Service Layer, Template Method en `ApiController`, Static Factory en `NotificationService::send()`, Strategy en las Policies, Fail-Fast con excepciones de dominio) y se agregó una tabla nueva de **malos olores** (obsesión por primitivos en los 6 campos de estado tipo string, ausencia de interfaces PHP, ausencia de máquina de estados explícita), no prevista originalmente en la plantilla pero exigida por la rúbrica (`project_instructions.md`, fila "Sección B" de la rúbrica incluye "SOLID, patrones de diseño, malos olores"). Los **Diagramas de Objetos** (`objetos_01.puml`, `objetos_02.puml`) instancian dos escenarios reales con IDs e valores coherentes entre sí: reserva confirmada con pago (`ReservationService::confirm()`) y reporte de incidente con notificación en cascada a admins+landlord (`ReportService::create()`). El **Diagrama de Estado** (`estado_01.puml`) documenta el ciclo de vida completo de `Reservations.status` (pending/confirmed/canceled, extraído de `ReservationService` y de la migración de la tabla), incluyendo la cancelación automática en cascada y el guard de conflicto de fechas; se agregó un estado "Finalizada" marcado explícitamente como nuevo/planificado (sugerido por HUG-08 del ERS, sin job en el código actual) para no inventar un estado inexistente sin dejarlo trazado. **Bug de compilación encontrado y corregido**: los caracteres `"` (comillas rectas) usadas en la prosa nueva colisionaban con los shorthands activos de `babel[spanish]` (p. ej. `"u` → ü), corrompiendo el texto compilado; se reemplazaron por las comillas tipográficas `` `` / `''` ya usadas en el resto del documento (y se corrigieron dos casos preexistentes en la plantilla que tenían el mismo problema, `seccionB.tex` línea 67 y `seccionC.tex` línea 24). Se evitó el placeholder genérico del macro `\DiagramaPagina` para objetos y estado (mismo criterio que en el punto 11), usando bloques manuales con la herramienta real y tabla de trazabilidad. Compilado sin errores ni overfull/underfull hbox (105 páginas).
14. **Diagramas de Estado restantes completados (2026-08-13)**, a pedido explícito del usuario ("completa los diagramas de estado faltantes"). Se identificaron 4 clases adicionales con comportamiento dependiente de estado (ya señaladas como candidatas en el punto 13 y en la tabla de Malos Olores de `seccionB.tex`): `StoreRooms.publication_status`, `Reports.status`, `Payments.payment_state` y `User.state`. Para cada una se exploró el controlador y el Form Request reales (`StoreRoomsController`, `StoreModerationController`, `ReportsController`, `PaymentsController`, `UserController`, `AdminController`, `SecurityController`, `AuthController`) antes de modelar, siguiendo el mismo criterio de fidelidad que en el punto 13. La exploración destapó **dos hallazgos reales de inconsistencia en el código**, documentados explícitamente en los diagramas (no solo mencionados, sino modelados como estados/transiciones marcados en rojo):
    - `Reports.status`: la migración declara `enum('status', ['pending','in_review','resolved'])`, pero `UpdateReportStatusRequest::rules()` valida `'status' => 'in:resolved,canceled'` y `ReportsController::updateStatus()` efectivamente escribe el string `"canceled"`, que **no pertenece al enum de la base de datos** (⚠️ corrección ver punto 16: se verificó empíricamente que esto falla igual en SQLite y en PostgreSQL — no es un problema exclusivo de producción). Además, `in_review` es un valor del enum que ningún endpoint del código actual asigna (estado muerto).
    - `User.state`: `AuthController::login()` solo llama a `Auth::attempt(email, password)` — no existe ningún middleware/guard que verifique `state`, por lo que una cuenta `pending` o `blocked` puede iniciar sesión con éxito hoy. Se documentó también que `AuthController::register()` no asigna `state` explícitamente (nace en `pending` por el `default` de la migración `create_user_table`, la tabla `user` real que referencian las FKs — no la tabla `users` muerta del scaffold).
    También se documentó que `StoreRooms.publication_status` y `StoreModeration.status` son dos fuentes de verdad **independientes** (mismo enum, sin sincronización en el código), y que `PaymentsController` **nunca invoca** `ReservationService::confirm()` (registrar un pago y confirmar la reserva son dos operaciones HTTP desacopladas, sin transacción del servidor que las una). Se agregó el color `gapbg` (`\definecolor{gapbg}{RGB}{250,219,216}`) en `main.tex` para la columna "Estado" de las tablas de trazabilidad, reservado específicamente para marcar inconsistencias detectadas (distinto de "Heredado"/"Nuevo"). Se corrigió además, de paso, el mismo bug de comillas rectas vs. shorthands de `babel[spanish]` (ver punto 13) encontrado en `secciones/contribuciones.tex` (preexistente en la plantilla, no introducido en este parcial). Compilado sin errores ni overfull/underfull hbox (113 páginas).
15. **`PLAN_CORRECCION_INCONSISTENCIAS.md` creado (2026-08-13/14)**, a pedido del usuario, como documento de diseño separado de la documentación LaTeX. Cubre plan de corrección para los 5 hallazgos del punto 14, en 3 fases. Tras una aclaración del usuario ("no podemos implementar la Fase 3 todavía, es parte de un sprint"), se verificó en `secciones/seccionA.tex` que HUA-03 (bloqueo/reactivación de cuentas) ya está en el Sprint Backlog (Sprint 1, 5 puntos, "Pendiente"), y se retiró la Fase 3 del plan de correcciones para no planificar la misma historia en dos documentos desincronizados — quedó solo como nota de referencia. El guard de login de la Fase 1.2 se acotó a `state === 'blocked'` explícitamente, sin tocar el significado de `pending`, para no adelantar decisiones de diseño de esa historia.
16. **Fases 1 y 2 del plan implementadas en `recursos/leodega_codigo/backend` (2026-08-14)**, a pedido explícito del usuario ("ejecuta el plan... comprueba que TODO el código funcione"). Se instaló el entorno (`composer install`, `npm install`), se estableció un baseline limpio (98/109 tests preexistentes — el resto son los agregados en este pase) y se corrigieron los 4 hallazgos de heredado (no se tocó el #5-B/HUA-03, confirmado fuera de alcance en el punto 15): (1) migración de `Reports.status` con SQL específico por motor (`Schema::getConnection()->getDriverName()`), verificada contra un PostgreSQL 16 real vía Docker desechable — se descubrió ahí que `$table->enum(...)->change()` de Laravel 12 genera SQL inválido en PostgreSQL (funciona en SQLite), y que el bug original fallaba en **ambos** motores, no solo en producción (corrección al punto 14 de este mismo archivo); (2) guard de login para `state === 'blocked'` (`AccountBlockedException`); (3) `StoreModerationService` sincronizando `StoreRooms.publication_status` con `StoreModeration`, integrado de forma no invasiva dentro de `StoreRoomsController::update()` (en vez de deprecar ese endpoint, como sugería el plan original, para no romper el contrato ya usado por tests/frontend) y también en `StoreModerationController::store()`; (4) `PaymentService` conectando `Payments` con `ReservationService::confirm()` dentro de una transacción, más `PaymentsPolicy` de ownership (hallazgo adicional: `/payments` no validaba que el pagador fuera el tenant dueño de la reserva). Verificación completa antes de dar por bueno: PHPUnit 109/109 (11 tests nuevos), PHPStan 0 errores (2 falsos positivos de propiedades dinámicas de Eloquent añadidos al baseline, mismo criterio que las 33 entradas ya existentes), PHPMD 0 violaciones, Pint 124 archivos conformes, Vitest del frontend 33/33 (sin cambios ahí). Se confirmó por `grep` que el frontend no llama a `/payments` todavía (cero riesgo de romper algo) y que `/admin/bodegas` (única UI que aprueba bodegas) ya está gateada a `role: admin` en `Routes/Role.tsx`, consistente con el nuevo chequeo de rol agregado en el backend. **Nada se commiteó ni se pusheó** — el usuario prepara y sube los commits sobre el working tree del repo `LeodegaPruebas`. Detalle completo de decisiones y desviaciones del plan original en `PLAN_CORRECCION_INCONSISTENCIAS.md`, §3–§10. *(Actualización: los commits se prepararon en el punto 17 — ver ahí.)*
17. **Commits preparados en `LeodegaPruebas` (2026-08-14)**: 6 commits (`Fix: la suite de tests no corría por falta de APP_KEY...`, `Fix: Reports.status aceptaba "canceled"...`, `Fix: login no validaba cuentas bloqueadas...`, `Fix: StoreRooms y StoreModeration quedaban desincronizados...`, `Fix: Payments no confirmaba la reserva...`, `Documentación: registra las correcciones...`), siguiendo el estilo ya establecido en el repo (prefijo `Fix:`/`Documentación:`, doc como commit final, sin `Co-Authored-By`). De paso se detectó que `PLAN_MAESTRO_REFACTORIZACION.md` (bitácora de la refactorización anterior, restaurado por el usuario tras aparecer borrado del working tree) necesitaba una entrada nueva siguiendo su propio formato de "Post-verificación", ya que documenta con el mismo rigor cada cambio sustantivo al código — se agregó, dejando explícito que este trabajo se originó fuera de ese plan (en `ingeParcial2/`, no en una fase de la refactorización). Working tree limpio y 109/109 tests confirmados después de separar todo en commits. **No se hizo push** — queda a cargo del usuario.
18. **Diagramas de Sección B/C rehechos tras los fixes del punto 16 (2026-08-14)**, a pedido explícito del usuario. Se identificaron 6 diagramas afectados: el **Diagrama de Clases** (faltaban `StoreModerationService`, `PaymentService`, `PaymentsPolicy`, `AccountBlockedException`, 2 casos nuevos de `NotificationType`, y el atributo `Reports.status` seguía mostrando el enum viejo) y los **5 Diagramas de Estado** (`Reservations`: nuevo disparador de `confirm()` desde `PaymentService`; `StoreRooms` y `Reports`: los hallazgos centrales que documentaban ya estaban corregidos, requirieron reescritura sustancial; `Payments` y `User`: igual, el gap que señalaban en rojo/verde-planificado ahora es heredado real). **Bug de PlantUML encontrado y corregido**: una nota del diagrama de estado de `Reports` perdía fragmentos enteros de texto sin error visible — la causa es que PlantUML trata cualquier línea que empiece con `'` (comilla simple), incluso dentro de un bloque `note`, como comentario y la descarta silenciosamente; se verificó por `grep` que ningún otro `.puml` del proyecto (incluidos los ya entregados) tenía el mismo patrón. Se evitó reintroducir comillas simples al inicio de línea en vez de intentar escapar el carácter. Las 6 imágenes se copiaron a `imagenes/`, se actualizaron las descripciones y tablas de trazabilidad de Secciones B y C (incluyendo las tablas de Patrones de Diseño y Malos Olores de \S3.2 para mencionar las 4 clases nuevas), y se recompiló sin errores ni overfull/underfull hbox (113 páginas, mismo conteo que antes).
19. **Ajuste de estilo en las descripciones (2026-08-14)**: el usuario pidió que las descripciones de los diagramas no narren "esto se corrigió"/"antes X, ahora Y" con fecha, sino que describan el sistema actual directamente. Se reescribieron los párrafos de "Descripción" de las Secciones B y C (quitando encabezados tipo "Actualización (2026-08-14):"/"Corregido (2026-08-14):" y fusionando el contenido en prosa continua) y también las notas dentro de los propios `.puml` (que tenían la misma redacción, visible en las imágenes). Las tablas de trazabilidad conservan celdas "Pendiente"/"Nuevo (planificado)" para gaps que siguen existiendo hoy — eso no es narrativa de corrección, es estado actual del sistema. Recompilado sin errores, mismo conteo de páginas (113).
20. **Bug de layout corregido en Sección B (2026-08-14)**: el usuario reportó una hoja en blanco después de la tabla de trazabilidad del diagrama de clases, seguida de la tabla SOLID desbordando el margen inferior, seguida de otra hoja en blanco. Causa raíz: las tablas de SOLID, Patrones de Diseño y Malos Olores usaban `\begin{table}[H]...\begin{tabular}` (float no paginable del paquete `float`), mientras que todas las demás tablas del documento (incluidas las de trazabilidad) ya usaban `longtable`. Al crecer el contenido de esas tres tablas (nuevas clases/patrones agregados en los puntos 18-19), la tabla SOLID completa ya no cabía en una sola página; al ser un `tabular` no divisible, LaTeX empujó el bloque completo (encabezado + tabla) a la página siguiente (dejando la anterior en blanco) y aun así se desbordó, por ser más alta que una página. Se convirtieron las tres tablas a `longtable` (mismo patrón `\endfirsthead`/`\endhead`/`\endfoot`/`\endlastfoot` con "(continuación)" que ya usan las tablas de riesgos y sprint backlogs de la Sección A), lo que les permite partirse naturalmente entre páginas. Verificado visualmente: sin hojas en blanco, sin desbordes, `\caption`/`\label` (Tabla 3.4/3.5/3.6) preservados y sus referencias cruzadas (`\ref{tab:solid}`, etc.) intactas. Recompilado sin errores, mismo conteo de páginas (113).
21. **Diagramas de Casos de Uso completados (2026-08-14)**, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`. Se exploró el código real como fuente de verdad de lo soportado (`backend/routes/api.php`, controladores, servicios `ReservationService`/`PaymentService`/`ReportService`/`StoreModerationService`, enum `NotificationType`, `AuthController::login` con `AccountBlockedException`) y se cruzó contra el ERS (`proyecto1P.tex`, HUG/HUC/HUA/HUE/HUL) para separar heredado 🟦 de nuevo 🟩. **Actores identificados (8):** Visitante, Usuario Registrado (abstracto, generaliza a los 3 roles), Cliente, Arrendador, Administrador, Administrador de Organización (nuevo, especializa a Cliente), y dos secundarios/externos: Servicio de Correo (heredado, `ResetPasswordNotification`) y Sistema de Inventario Externo (nuevo). Decisión fundamentada: el pago es simulado (ERS HUC-05, fuera de alcance) → **no** se modela pasarela como actor externo; Leodeguita (HUL) se documenta como canal móvil que reutiliza los mismos UC, no como UC duplicados. **Decisión de diseño:** en vez de un único diagrama con ~43 UC (ilegible, la rúbrica penaliza hasta -50 por legibilidad), se hicieron **6 diagramas**: 1 general de contexto (`diagrama_casos_uso.puml`, actores+jerarquía+9 subsistemas) + 5 detallados por subsistema (`casos_uso_01..05.puml`: Autenticación/Cuenta/Comunicación; Cliente; Arrendador; Administrador; Módulos nuevos Organizaciones+Inventario). En `seccionB.tex` se reemplazó el placeholder por: intro, tabla de actores, los 6 diagramas (cada uno con descripción, herramienta PlantUML y tabla de trazabilidad, patrón manual igual a Componentes/Objetos), un **catálogo consolidado de 43 UC** (longtable `footnotesize`) y **11 fichas extendidas** (formato `ref21`) de los UC transaccionales medulares (Registrarse, Login, Reservar, Confirmar pago, Confirmar reserva landlord, Publicar bodega, Reportar incidente, Moderar publicación, Bloquear/Reactivar cuenta, Invitar a organización, Ver inventario sincronizado). **Dos bugs encontrados y corregidos:** (1) en `babel[spanish]` los caracteres `<` y `>` son *shorthands activos* (generan guillemets «»); escribir `{<}{<}include{>}{>}` rompe la compilación con "Argument of \\language@active@arg< has an extra }" — se usó `\guillemotleft include\guillemotright` (notación UML correcta). Misma familia que el bug de comillas de los puntos 13/14. (2) Overfull hbox por `\texttt{Clase::metodo/metodo}` largos que no cortan en columnas angostas → se convirtieron a `\url{}` (corta en `/` y `:`, mismo criterio que las rutas ya existentes) y se envolvió la sección en `\sloppy`/`\fussy`. **Entorno:** esta máquina no tenía `dot`/Graphviz ni toolchain LaTeX (a diferencia de sesiones previas); se descargó `plantuml.jar` v1.2026.6 a `~/.local/share/plantuml/` y se renderizó con el flag CLI `-Playout=smetana` (motor Java puro, sin tocar los `.puml` para mantenerlos consistentes con los otros diagramas que usan `dot`); se compiló con el binario portable `tectonic` 0.17.0. Verificado: compila sin errores (exit 0), **135 páginas**, sección nueva sin overfull significativo (máx 9pt), diagramas legibles confirmados visualmente sobre el PDF. `main.pdf` actualizado.
22. **Ajustes de casos de uso a pedido del usuario (2026-08-14)**: (a) el usuario editó los 6 `.puml` quitando los `legend` y las `note` explicativas, y el "(simulado)" de "Confirmar pago"; se re-renderizaron los 6 PNG con Smetana y se alineó `seccionB.tex` (etiqueta del UC "Confirmar pago" en descripción/catálogo/ficha; se conservó "pago simulado (HUC-05)" solo como hecho de negocio en la prosa). (b) Se rehízo el **formato de las 11 fichas** al pedido por el usuario: rejilla de 4 columnas con bordes completos, etiqueta en negrita/derecha/fondo gris, filas 2-3 con dos pares etiqueta-valor (Creado por | Actualizado por; Fecha de creación | Última revisión), listas numeradas en pre/post/flujo/alternativos (o "—" si vacío), requerimientos en viñetas de guión, y pie en cursiva centrado "Tabla N. \<nombre\>.". Se creó un **entorno reutilizable `casouso`** en el preámbulo de `main.tex` (columntype `K` gris/negrita/derecha, `\cuvalwidth` por `\dimexpr` para el span de 3 columnas, y macros `\CUnombre`/`\CUmeta`/`\CUfechas`/`\CUdescripcion`/`\CUactores`/`\CUprecondiciones`/`\CUpostcondiciones`/`\CUflujo`/`\CUalternativos`/`\CUexcepciones`/`\CUrequerimientos`) para que cada ficha solo cambie contenido. Los campos "Disparador"/"Reglas de Negocio"/"Supuestos" del formato anterior se refundieron en Descripción/Flujo/Requerimientos. Metadatos: autores = "Equipo Leodega", fechas = 14/08/2026 (fecha real de redacción, reemplazable). **Gotcha de numeración:** en este entorno todo `longtable` incrementa el contador `table` aunque no tenga `\caption` (por eso las tablas de trazabilidad ya "comían" números y el catálogo quedó en Tabla 3.7); el `\refstepcounter{table}` puesto en el entorno hacía contar doble (3.9/3.11/3.13…). Se quitó y cada ficha usa el número que su propio `longtable` avanza → consecutivo 3.8–3.18. Verificado: exit 0, **138 páginas**, fichas sin overfull, formato confirmado visualmente.

---

23. **32 Diagramas de Secuencia completados (commit `85994b2`, 2026-08-14, por Johao)**: se agregaron los 32 `.puml` (`recursos/diagramas_uml/secuencia_1..32.puml`) y sus PNG en `imagenes/`, reemplazando el placeholder de `seccionB.tex`/`seccionC.tex`. Cubren 20 flujos heredados (registro, login, reservas con detección de conflictos, pagos ligados a confirmación de reserva, reportes, moderación, mensajería, etc.) y 12 flujos nuevos planificados (bloqueo de cuentas, permiso de bomberos, panel admin, organizaciones, inventario externo, Leodeguita), cada uno con descripción, fuente y marca heredado/nuevo. Esta entrada documenta retroactivamente ese commit, que no había quedado registrado en este archivo.
24. **Matrícula, herramienta ágil y contribuciones completadas (2026-08-17), a pedido explícito del usuario**: (a) portada (`secciones/portada.tex`) actualizada con la matrícula real de Darwin (202315297) y Geovanny (202312450); Carla y Johao quedan con `[ID]` porque el usuario no las proporcionó todavía (no se inventa, ver §4). (b) `secciones/seccionA.tex` línea ~145: se reemplazó el placeholder de herramienta de gestión ágil por **Jira** (dato confirmado por el usuario), manteniendo el estilo `\textit{}` usado en el resto del documento para valores reales que reemplazan placeholders. (c) `secciones/contribuciones.tex`: se llenaron los 4 nombres completos y su contribución específica (Darwin: despliegue y componentes; Carla: clases, estado y objetos; Geovanny: casos de uso y actividad; Johao: secuencia y colaboración), tomados textualmente de lo indicado por el usuario. El `%` de aporte de cada integrante se dejó como placeholder `[25\%]` porque el usuario no lo especificó — no se asume reparto igualitario sin confirmación, ya que debe ser consistente con la co-evaluación individual (nota al pie de la tabla). **No se tocaron** los diagramas de Actividad ni de Colaboración de la Sección C: el usuario indicó explícitamente que sus compañeros los están elaborando en paralelo. Recompilado con `latexmk -pdf` sin errores nuevos (135 páginas, mismos warnings preexistentes en Anexos).

---

25. **Matrícula restante y % de aporte completados (2026-08-17), a pedido explícito del usuario**: (a) portada (`secciones/portada.tex`) completada con la matrícula real de Carla (202210514) y Johao (202201091) — las 4 matrículas quedan llenas. (b) `secciones/contribuciones.tex`: el usuario indicó explícitamente delegar la decisión del `%` de aporte ("gestiónalo tú") y aclaró que, además de los diagramas ya asignados individualmente (ver punto 24), el resto del documento se elaboró **como equipo**. Se optó por **25\% para cada integrante**: cada uno tiene asignado un par de categorías de diagramas de alcance comparable (despliegue+componentes / clases+estado+objetos / casos de uso+actividad / secuencia+colaboración), y el resto del contenido (información general, riesgos, sprint backlogs, cronograma, catálogo de casos de uso, tablas de SOLID/patrones/malos olores, trazabilidad) es trabajo conjunto sin una asignación individual documentada en ninguna fuente — repartirlo desigualmente habría sido inventar un dato, así que se usó el reparto equitativo como el más defendible sin más información. Se agregó una oración a la intro de `contribuciones.tex` aclarando esta distinción (diagramas individuales vs. resto conjunto) para que la tabla no se lea como si cada quien solo hubiera hecho sus diagramas. Recompilado con `latexmk -pdf` sin errores nuevos (135 páginas).

---

26. **Diagrama de Clases dividido en 10 partes (2026-08-17), a pedido explícito del usuario** ("solo se aprecia bien haciendo zoom... dividirlo en pequeñas partes"): el `diagrama_clases.puml` monolítico (35 clases/enums, 9 paquetes) se reemplazó por 10 fuentes PlantUML nuevas en `recursos/diagramas_uml/`: `diagrama_clases_00_paquetes.puml` (vista general de contexto, un box por paquete con sus relaciones principales, sin atributos) y `diagrama_clases_01..09_*.puml` (uno por cada paquete ya existente en el diagrama original: Usuarios y Roles, Almacenes, Reservas y Pagos, Comunicación, Moderación y Reportes, Servicios de Aplicación, Policies, Excepciones, Nuevo planificado), cada uno con sus atributos/métodos completos y las clases externas que participan en alguna relación mostradas como cajas grises `<<Referencia>>` (sin atributos) que remiten al diagrama donde están definidas — así cada imagen es legible a tamaño de página, sin perder ninguna relación del diagrama original. `seccionB.tex` se reestructuró de una sola `\section` con una figura a 1 intro + 10 `\subsection` (general + 9 detalle), cada una con su propia tabla de trazabilidad (se dividieron las filas de la tabla original, que ya estaba agrupada por paquete, sin perder ni cambiar ninguna fuente citada). **Bug propio detectado y corregido antes de dar el trabajo por terminado:** el primer intento puso números de sección hardcodeados (`\S3.2.1`, etc., incluidos como texto dentro de las imágenes PlantUML) asumiendo que el diagrama general sería `3.2.1` y el resto `3.2.2..3.2.10`; al compilar, el diagrama general en sí ya ocupaba `3.2.1`, corriendo la numeración real un lugar (`Usuarios y Roles` quedó en `3.2.2`, no `3.2.1`). Se corrigió de raíz: en el `.tex` las referencias cruzadas ahora usan `\label`/`\ref` (auto-consistentes ante cualquier renumeración futura) en vez de números fijos; en las imágenes (que no pueden usar `\ref`) se reemplazaron los números de sección por la numeración manual y estable "Diagrama de Clases NN", que coincide con el nombre de archivo y el título de cada subsección y no depende del contador de LaTeX. Se verificaron visualmente los 10 PNG renderizados (PlantUML v1.2026.6 + Graphviz/`dot`) antes de insertarlos. Recompilado sin errores nuevos ni referencias sin resolver (147 páginas, antes 135). El `diagrama_clases.puml`/`.png` monolítico se eliminó (`git rm`, sin commitear) por quedar completamente superseded.

---

27. **`git pull` recibido y verificado (2026-08-17), a pedido explícito del usuario** ("he hecho un pull con nueva info... verifica qué queda pendiente"): fast-forward de `85994b2` a `fe7cf29` ("Diagramas de actividad", autora `cgutierrez05`). Trae los **12 Diagramas de Actividad** completos (`recursos/diagramas_uml/actividad_01..12.puml`, `imagenes/actividad_01..12.png`, contenido real en `secciones/seccionC.tex`) — quedan solo los de Colaboración/Comunicación (3) como placeholders en Sección C. Se verificó que el pull no tocó ningún archivo con cambios locales sin commitear de esta sesión (portada, contribuciones, seccionA, seccionB, PROGRESS.md, el split del diagrama de clases) — no hubo conflictos ni marcadores `<<<<<<<` que resolver. Se recompiló `main.pdf` completo (167 páginas, sin errores ni referencias sin resolver) para reflejar ambos aportes juntos. **Hallazgo nuevo (pendiente de decisión del usuario):** el commit trajo `recursos/Leodega-codigo` (con mayúscula y guion, distinto de `recursos/leodega_codigo/` que sí está en `.gitignore`) como un *gitlink* huérfano (entrada de submódulo sin `.gitmodules`, carpeta vacía en el working tree) — mismo tipo de error que ya se había limpiado una vez en el commit `ee8c5d8` ("repo git anidado, solo referencia local"). No se tocó porque no fue lo que pidió el usuario en este turno; queda anotado en pendientes (§5) para que decida si lo limpiamos (probablemente `git rm --cached` + agregar el patrón a `.gitignore`).

---

28. **Gitlink huérfano `recursos/Leodega-codigo` limpiado (2026-08-17), a pedido explícito del usuario**: `git rm --cached -r recursos/Leodega-codigo` (quita la entrada 160000 del índice; no había contenido real que perder, la carpeta ya estaba vacía en el working tree) y se borró la carpeta vacía resultante. Se agregó `recursos/Leodega-codigo/` a `.gitignore` (mismo patrón que ya tenía `recursos/leodega_codigo/`) para que no vuelva a colarse por accidente si alguien clona el código ahí de nuevo con esa mayúscula. Cambio dejado sin commitear, igual que el resto de la sesión.

---

29. **Diagramas de Colaboración/Comunicación completados (2026-08-17), a pedido explícito del usuario**, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`: se generaron 6 diagramas de comunicación UML (`recursos/diagramas_uml/comunicacion_01..06.puml`), reutilizando como fuente de verdad los mensajes ya validados contra el código real en los Diagramas de Secuencia correspondientes (no se re-exploró el código desde cero; se tradujo la interacción temporal ya documentada a la vista estructural de colaboración): 01 Inicio de sesión (↔ Secuencia \#2), 02 Reservar bodega (↔ \#11), 03 Confirmar reserva con cancelación en cascada (↔ \#12), 04 Procesar pago y confirmar reserva (↔ \#14), 05 Reportar incidente con notificación en cascada (↔ \#16), 06 Bloquear cuenta de usuario (↔ \#21, nuevo planificado ERS HUA-03). Cada uno con numeración jerárquica de mensajes (`N`, `N.M`, `N.M.P`) y tabla de trazabilidad. **Notación:** PlantUML no tiene un tipo de diagrama de colaboración nativo; se usó el modo `object`/`database` con `allowmixing` (mezclar actores `object <<Actor>>` con objetos de dominio) y mensajes como flechas `->` etiquetadas `N: mensaje()`, patrón estándar de la comunidad PlantUML para este tipo de diagrama.

    **Iteración de layout (varios intentos antes de aceptar el resultado):** el primer intento con el layout por defecto de PlantUML era ilegible sin zoom (texto de mensajes superpuesto entre sí y con las cajas de objetos) — exactamente el problema que se pidió evitar. Causas raíz identificadas y resueltas una por una: (1) mezclar `actor` con `object` en el mismo diagrama rompe la detección de tipo de diagrama de PlantUML (`Assumed diagram type: component`) — solución: usar `object "nombre" as X <<Actor>>` para los actores humanos también, con su propio color en el `skinparam`, en vez de `actor`. (2) Mezclar `object`/`database` sin la directiva `allowmixing` falla con "Use 'allowmixing' if you want to mix classes and other UML elements" — se agregó `allowmixing` a los 6 archivos. (3) Escribir texto explicativo (p. ej. "N, N.M = orden...") como línea suelta dentro de un bloque `legend...endlegend` rompe la sintaxis de tabla del legend — se movió a un `note` aparte. (4) Cualquier línea dentro de un bloque `note` que empieza con `*` se interpreta como viñeta de lista (PlantUML la reemplaza por `•`), no como el asterisco literal que se quería para marcar mensajes iterativos — se reescribió para que el `*` nunca sea el primer carácter de la línea (p. ej. `(*) = mensaje iterativo`). (5) Cuando el mismo par de objetos tiene **dos o más mensajes** dibujados como flechas separadas en la misma dirección, PlantUML dibuja curvas casi idénticas y sus etiquetas de texto quedan una encima de la otra — la solución no fue de layout sino de **contenido**: se consolidó cada par de mensajes redundantes en una sola flecha con una etiqueta de varias líneas (p. ej. `RS -> RDB : 3.1: findOverlapping(); [sin conflicto] insert(status='pending')` en vez de dos flechas separadas), eliminando el problema de raíz en vez de intentar separar visualmente dos flechas que decían esencialmente lo mismo. (6) Los nodos de tipo `database` (a diferencia de `object` simple) tienden a "derivar" hacia el extremo derecho del diagrama en el layout automático de Graphviz, alejándose de quien los usa — se agruparon con `together { object X \n database Y }` para forzarlos a quedar adyacentes a su "dueño" en el diagrama; con el motor `ortho` (usado en los diagramas de clases de la Sección B) esto funciona con un solo bloque `together` pero **falla** (`IllegalStateException` de Graphviz) si se combinan varios bloques `together` a la vez, así que se abandonó `linetype ortho` para estos diagramas y se usó el motor de curvas por defecto (spline), donde sí se pudieron usar varios bloques `together` simultáneamente sin errores (confirmado en `diagrama_clases_06_servicios.puml` de la Sección B esto no aplicaba porque ese caso solo necesitaba un bloque). (7) Un `note` "flotante" (sin `top of`/`bottom of` que lo ancle a un objeto) posicionado cerca del extremo derecho del lienzo quedaba recortado por el borde de la imagen en algunos diagramas (el PNG generado no ampliaba su propio canvas para incluirlo completo) — se ancló cada nota explicativa al primer actor (`note top of <actor>`), que por diseño siempre queda dentro de los límites del diagrama al ser el elemento más a la izquierda.

    Verificado uno por uno: los 6 renderizan sin errores y sin texto superpuesto (revisión visual de cada PNG a resolución completa, con recortes ampliados en las zonas de mayor densidad de mensajes). Compilado el documento completo con `latexmk -pdf`: 170 páginas, sin errores ni referencias sin resolver (se agregó `\label{sec:secuencia}` a la Sección de Diagramas de Secuencia para poder referenciarla con `\ref{}` desde la nueva intro de Colaboración, en vez de un número de sección fijo — mismo criterio anti-frágil ya aplicado en el punto 26 para el split del diagrama de clases).

---

30. **Diagramas de Colaboración reorientados a vertical (2026-08-17), a pedido explícito del usuario** ("horizontalmente no se llegan a apreciar muy bien"): los 6 diagramas del punto 29 originalmente salían muy anchos y bajos (formato apaisado), forzando zoom horizontal para leerlos en una página en formato carta/A4. Se investigó por qué los *hints* de dirección de PlantUML (`-down-`, `-right-`, etc.) no tenían ningún efecto inicialmente (se probó exhaustivamente con casos mínimos aislados): la causa raíz es que un *hint* de dirección puesto en una línea estructural (`--`) queda **anulado** si existe, entre el mismo par de objetos, una segunda arista (la flecha de mensaje `->`) sin su propio *hint* — PlantUML/Graphviz promedia o descarta la dirección cuando hay múltiples aristas contradictorias entre el mismo par. La solución fue eliminar las líneas estructurales `--` sueltas y poner el *hint* de dirección directamente en cada flecha de mensaje (p. ej. `AC -down-> AS : 2.3: mensaje()` en vez de `AC -- AS` más `AC -> AS : 2.3: mensaje()` por separado); para los pocos enlaces sin mensaje propio (p. ej. `NS -- T` en el diagrama 03, donde la notificación ya viaja implícita en otro mensaje) se dejó la línea `--` pero también con su *hint* de dirección explícito. Con eso, cada diagrama quedó con una cadena principal vertical (`-down-`) y las ramas laterales (bases de datos, actores secundarios) hacia los costados (`-right-`/`-left-`), igual que ya se hacía para el diagrama de clases por paquetes de la Sección B (punto 26), pero ahí no hizo falta este ajuste porque esos diagramas no combinaban una línea estructural y una flecha de mensaje sobre el mismo par. Se corrigió además un caso puntual en el diagrama 05 (Reportar incidente): el primer intento de poner `RS -left-> RE` producía un lazo largo y feo porque `RS` ya era el elemento más a la izquierda de su fila — no había espacio real a su izquierda — así que se cambió a `-right->` para ambas bases de datos (`RDB` y `RE`), dejando que el bloque `together` ordene su posición relativa. Verificados visualmente los 6 PNG resultantes (incluyendo recortes ampliados de las zonas más densas) antes de integrarlos. Recompilado con `latexmk -pdf` sin errores ni referencias sin resolver (172 páginas, antes 170 — el formato vertical usa más alto por diagrama pero el mismo o menor ancho, y encaja mejor en las páginas verticales del documento).

---

31. **Pulido de formato: "Herramienta utilizada" pareja, centrado de fichas de casos de uso y eliminación de todos los overfull hbox reportados (2026-08-17), a pedido explícito del usuario** (lista concreta de desbordes vistos en el PDF compilado). Se usó `latexmk`/`pdflatex` local para generar `main.log` y se parseó con un script Python (el log está en Latin-1, no UTF-8 — `grep` normal lo trataba como binario y no reportaba coincidencias; hubo que forzar `grep -a` o leer el archivo con Python) para localizar cada "Overfull \hbox" con su archivo/línea de origen real y el texto exacto que se desborda, en vez de adivinar por inspección visual. Cambios:
    - **Herramienta de modelado utilizada, formato parejo**: 23 de 28 apariciones ya usaban la forma con paréntesis informativo (`PlantUML v1.2026.6 (motor Graphviz/dot)` o variantes); las 5 que faltaban (`casos_uso_01`..`05` en `seccionB.tex`) se completaron con `(motor Graphviz/\texttt{dot})` para igualar al resto, en vez de quitarle el paréntesis a la mayoría.
    - **Diagramas de Secuencia (32) sin "Herramienta" real**: se descubrió que los 32 usaban el macro genérico `\DiagramaPagina` de `main.tex`, cuyo texto de herramienta es el placeholder literal `[Ej. Visual Paradigm, StarUML, Draw.io]` — nunca se había llenado. Se creó un macro nuevo `\DiagramaPaginaHerramienta` (con un 5.º argumento para el nombre del `.puml` fuente) que genera el texto real de PlantUML y envuelve la Descripción en `\sloppy` para evitar overfull con identificadores de código largos; se convirtieron los 32 `\DiagramaPagina{...}` de `secuencia_1..32` a la nueva macro con un script Python (regex por línea, ya que cada llamada ocupa una sola línea física). El macro original `\DiagramaPagina` se dejó intacto porque `anexos.tex` todavía lo usa para las pantallas del prototipo (placeholder pendiente, fuera de alcance).
    - **Centrado vertical roto en las fichas de caso de uso**: en las filas Precondiciones/Postcondiciones/Flujo/Flujos Alternativos/Requerimientos (las que usan `enumerate`/`itemize`), el contenido quedaba pegado arriba de la celda con espacio muerto abajo, mientras la etiqueta sí se veía centrada. Se armó un MWE aislado (`/tmp/.../mwe/`) para diagnosticar: confirmado que es una incompatibilidad conocida entre listas (`enumerate`/`itemize`) y el centrado vertical de las columnas `m{}` de `array` — el fix validado empíricamente es envolver el contenido en `\begin{minipage}{\linewidth}...\end{minipage}` dentro de la celda. Se aplicó en las 5 macros de `main.tex` (`\CUprecondiciones`, `\CUpostcondiciones`, `\CUflujo`, `\CUalternativos`, `\CUrequerimientos`); `\CUexcepciones` no lo necesitaba (nunca lleva lista) y se dejó igual.
    - **Párrafo inicial del Diagrama de Clases** (`seccionB.tex`, "El diagrama modela..."): envuelto en `{\sloppy...\par}` — eliminó los 3 overfull (`DuplicateRatingException`/`RuntimeException`, `AccountBlockedException`, `StoreModerationService`/`PaymentService`/`PaymentsPolicy`/`AccountBlockedException`) que antes sumaban ~188pt de desborde.
    - **Tabla SOLID 3.28, fila D -- Dependency Inversion**: `ReservationsController::store(StoreReservationRequest...)` no tenía ningún punto de ruptura entre `store(` y `StoreReservationRequest` (van pegados, sin espacio); se insertó un guion discrecional `\-` ahí.
    - **Tabla de Patrones de Diseño (3.29), columna Categoría**: `M{2.4cm}` era más angosta que la palabra "Comportamiento" (aparecía 3 veces) y "Arquitectónico"; se ensanchó a `M{3.2cm}` y se redujo la columna de Justificación de `L{8.8cm}` a `L{8cm}` para compensar (mismo ancho total de tabla).
    - **23 párrafos "Descripción:" de Actividad (12), Colaboración (6) y Estado (5) en `seccionC.tex`**: se envolvieron en `{\sloppy...\par}` con un script Python (mismo criterio: cada uno es una sola línea física, detectada por el prefijo exacto `\noindent\textbf{Descripción:}`). Esto resolvió, entre otros, los 4 casos puntuales que el usuario señaló (Actividad 3 "pending", Actividad 8 "MessageController"/"Controllers/", Actividad 10 "StoreModerationService", Actividad 12 "SincronizacionService", Estado Reservations "ReservationConflictException"/"confirm()", Estado Reports "UpdateReportStatusRequest") y decenas de casos no señalados explícitamente pero de la misma naturaleza.
    - **Verificación de los 32 Diagramas de Secuencia**: tras el macro nuevo quedó un único overfull residual (12.5pt) en la Secuencia 30 (`SincronizacionService::sincronizar()` pegado al inicio del párrafo, sin margen para acomodarlo ni con `\sloppy`); se resolvió con un `\-` extra después de `::`.
    - **Fuera de alcance, dejado igual a propósito**: overfull menores preexistentes en tablas de trazabilidad no mencionadas por el usuario (p. ej. `ReportService::notifyAdminsAndLandlord()` en la tabla de Actividad 06, o la fila "Enum de la columna" en la tabla de Estado de Reports) y en la sección de Objetos (`seccionB.tex` ~718-730) — se verificó que ya existían antes de esta sesión y no fueron parte de la lista del usuario; tocarlos habría sido exceder el pedido.
    - Verificado con el mismo parseo de log: de 147 `Overfull \hbox` originales en `seccionB.tex`/`seccionC.tex` quedaron 83, ninguno dentro de las 32 descripciones de Secuencia, las 12 de Actividad, las 5 de Estado, el párrafo del Diagrama de Clases, ni las dos tablas señaladas. Confirmado también visualmente (capturas de página vía `pdftoppm` de la ficha UC-14, la intro de clases, los estados de Reservations/Reports, la Secuencia 30, la Actividad 08 y la tabla de Patrones). Compilado con `latexmk -pdf`: 170 páginas (antes 172; la reducción es por el `\sloppy` permitiendo mejor aprovechamiento de línea, no por contenido eliminado), sin errores ni referencias sin resolver. Cambios sin commitear.

---

32. **Espaciado vertical en las fichas de caso de uso (2026-08-17), a pedido explícito del usuario** ("dale un poco más de espacio superior e inferior"): tras el fix del punto 31 (minipage para centrar listas dentro de las celdas), el contenido quedaba centrado pero pegado a los bordes horizontales de la celda sin ningún margen. Se armó un MWE aislado para probar la técnica antes de tocar el documento real: `\vspace{Npt}` al **inicio** de un `minipage` se descarta silenciosamente (mismo mecanismo de supresión de espacio que al inicio de página, aunque un minipage no es una página) — confirmado insertando 20pt de prueba y viendo que no producía ningún cambio visual; en cambio, un `\rule{0pt}{Npt}` (regla invisible de ancho cero) sí fuerza esa altura porque no es glue descartable. La solución final combina `\rule{0pt}{4pt}` al inicio y `\vspace{4pt}` al final (este sí funciona porque no está al inicio del box) en las 5 macros `\CUprecondiciones`/`\CUpostcondiciones`/`\CUflujo`/`\CUalternativos`/`\CUrequerimientos` de `main.tex`. Verificado visualmente sobre la ficha UC-14 (página 52): margen simétrico visible, sin overflow. Recompilado sin errores (172 páginas, antes 170 — las fichas ahora ocupan un poco más de alto).
33. **Desborde de "Enum de la columna (pending/resolved/canceled)" en la tabla de trazabilidad del Diagrama de Estado -- Reports (2026-08-17), a pedido explícito del usuario**: era uno de los overfull dejados fuera de alcance a propósito en el punto 31 (tabla, no descripción). La causa era que el texto entre paréntesis está formado por tres `\texttt{}` unidos con `/` literal sin espacios (`\texttt{pending}/\texttt{resolved}/\texttt{canceled}`), un bloque de ~201pt totalmente sin puntos de ruptura dentro de una columna `L{4cm}`≈113pt — ni ensanchar la columna ni `\sloppy` lo hubieran arreglado, porque ninguno de los dos inserta puntos de corte dentro de un token atómico. Se reemplazaron los `/` literales por `\slash` (comando estándar de LaTeX que imprime "/" y además permite el salto de línea justo después), sin tocar el resto de la tabla. Verificado visualmente (página 159): el texto ahora envuelve dentro de la columna sin desbordarse. Recompilado sin errores (172 páginas).
34. **Hallazgo de `recursos/informacion-general/` y Anexo completado (2026-08-17), a pedido explícito del usuario** ("mira qué puedes hacer con ello"): el usuario agregó una carpeta nueva (con guion, distinta de la antigua `recursos/informacion_general/` con guion bajo, cuyo `proyecto1P.tex`/`.pdf` ya no existen en disco — aparecen como `D` en `git status`, reemplazados por esta carpeta nueva) que resultó ser el **ERS completo del primer parcial en LaTeX** (`main.tex`, 1893 líneas, en inglés): Introducción, Alcance, resumen de RF/RNF, las 38 historias de usuario completas en formato Volere (prioridad MoSCoW, estimación, sprint, criterios de aceptación con escenarios Contexto/Evento/Resultado), 11 RNF detallados con criterios de validación, un capítulo "System Prototype" con 34 capturas reales del prototipo (React 19 + TypeScript + Tailwind CSS, desplegado en `https://darwin4050e.github.io/PrototipoLeodega/`) organizadas por rol, y un capítulo "Acceptance Letter" con `CartaP1.jpg`/`CartaP2.jpg`: el acta de conformidad firmada por el stakeholder del primer parcial. Se verificó por muestreo que los puntos/sprint de varias historias (HUG-01, HUC-05, HUA-03, HUE-04, HUL-01) coinciden exactamente con los ya usados en el Sprint Backlog de `seccionA.tex`, confirmando que es la misma fuente canónica (y de paso reveló el nombre completo del cliente por su firma: **Ing. Leonardo Castro**, Product Owner de Leodega — se actualizó `secciones/portada.tex`, que hasta ahora solo tenía el fallback "Leonardo" sin apellido, ver §3). Se confirmó con el usuario, vía pregunta directa, dos decisiones antes de tocar `anexos.tex`: (a) usar `CartaP1.jpg`+`CartaP2.jpg` (convertidos a `imagenes/acta_conformidad_ers.pdf` con ImageMagick `convert -density 150`) como el acta de conformidad del Anexo — dejando explícito que **no** cubre el acta del cuerpo principal de este 2do parcial (Secciones A/B/C), que sigue pendiente por separado; y (b) traducir y adaptar el ERS completo al español (no dejarlo en inglés ni resumirlo). Trabajo dividido en tres partes: (1) el acta, convertida y verificada visualmente (página con la firma de Leonardo Castro); (2) la sección de Prototipo (`secciones/anexo_prototipo.tex`, nuevo, `\input` desde `anexos.tex`), armada directamente por Claude: se copiaron las 34 capturas a `imagenes/`, se tradujeron los captions, y se creó un diagrama de flujo de ventanas real en PlantUML (`recursos/diagramas_uml/flujo_ventanas.puml`, un diagrama de actividad con `switch`/`fork` por rol) ya que la fuente no traía un diagrama de flujo como tal, solo las capturas organizadas por sección; el campo "Herramienta utilizada" se llenó con el dato real (React/Tailwind, prototipo funcional desplegado, no una herramienta de mockups) en vez de inventar Figma; (3) el ERS mejorado (`secciones/anexo_ers.tex`, nuevo), delegado a un agente fork con instrucciones muy explícitas (rangos de líneas exactos de la fuente, estructura Volere destino, patrón de tabla real del proyecto en vez del `booktabs` de la fuente, y los gotchas de `babel[spanish]` ya conocidos de esta sesión — comillas rectas, `<`/`>` sueltos) dado el volumen (38 historias + 11 RNF + resumen); el resultado (821 líneas) se revisó íntegramente antes de integrarlo: 0 comillas rectas, 0 `<`/`>` sueltos, 0 `_`/`%`/`#` sin escapar, entornos balanceados, y los mismos 38 IDs con los mismos puntos verificados contra `seccionA.tex` (0 discrepancias). Único ajuste post-revisión: se ensanchó la columna "Referencia" de la tabla resumen de RNF (`M{2cm}`→`M{2.3cm}`, compensado achicando la columna de nombre) por un overfull de 5pt en el header. Compilado completo sin errores ni referencias sin resolver: **232 páginas** (antes 172), 16.4MB (antes 7.5MB, por las 34 capturas + el acta). Nota de estilo aceptada: las referencias cruzadas `\S\ref{}` a historias individuales dentro de un mismo rol (p. ej. las 8 de HUG) todas apuntan a la misma subsección (`§A.3.1`) porque las historias son bloques `\textbf{}` dentro de una única `\subsection` por rol, no subsecciones individuales — es una imprecisión de navegación menor y aceptada (no un error), no se convirtieron en subsecciones numeradas para no inflar el índice con 38 entradas nuevas.
35. **Commits preparados y pusheados (2026-08-19), a pedido explícito del usuario** ("prepara los commits necesarios para subir los cambios. No te incluyas como coautor"): se dividió el working tree acumulado de la sesión anterior en 5 commits temáticos, separando hunks dentro de un mismo archivo (`main.tex` tenía dos cambios independientes — la macro `\DiagramaPaginaHerramienta` y las macros `\CU*` de centrado — separados con `git diff`/`git apply --cached` sobre patches extraídos por hunk, no con edición manual) para que cada commit sea revisable por separado: `72788e6` (desbordes + herramienta uniforme), `18b90ea` (centrado de fichas de caso de uso), `1d49c38` (nombre del cliente en portada), `a10554e` (Anexo completo), `c52eb2c` (PDF + bitácora) — siguiendo el estilo ya establecido en el repo (imperativo, sin coautoría). El usuario hizo el `git push` él mismo entre sesiones (confirmado por `HEAD` == `origin/main` al retomar).
36. **Acta de conformidad del cuerpo principal resuelta (2026-08-19), a pedido explícito del usuario**: el usuario colocó `imagenes/acta_conformidad_final.pdf` (2 páginas, firmada por Leonardo Castro, Product Owner de Leodega, fechada 16/08/2026 — distinta de `acta_conformidad_ers.pdf`, que es la del primer parcial). Se renombró a `imagenes/acta_conformidad.pdf` (nombre que ya espera `main.tex` línea 236 vía `\IfFileExists`). **Gotcha de compilación:** `latexmk` reportó "Nothing to do" en el primer intento porque no conocía el archivo nuevo como dependencia (no existía en la corrida anterior, por lo que no estaba en el `.fls`) y no lo detectó pese a que el `\IfFileExists` sí cambiaría de rama; hubo que forzar con `latexmk -pdf -g` (flag `-g` = rebuild incondicional) para que efectivamente se incluyera. Verificado visualmente (extracción de página con `pdftoppm`): firma correcta, contenido de aceptación específico de este 2do parcial (no el texto del ERS). Advertencia benigna de pdfTeX ("found PDF version 1.7, but at most version 1.5 allowed") por incluir un PDF más nuevo del que pdfTeX soporta nativamente — no corrompe el contenido, solo lo baja de versión al insertarlo. Compilado sin errores: 234 páginas (antes 232). Cambio sin commitear todavía.

---

## 3. Decisiones y convenciones adoptadas (el "por qué")

- **Los IDs de historia (HUG/HUC/HUA/HUE/HUL) y los IDs de actividad (HST-XXX) son intencionalmente independientes.** `proyecto1P.tex` los usa como dos numeraciones distintas: HU-XX para las historias de usuario (Sección de Requerimientos) y HST-XXX para las actividades del análisis PERT/CPM. Se verificó que hay correspondencia 1 a 1 entre ambos conjuntos (38 = 38, mismas duraciones/sprint), pero se documentan por separado (con nota al pie en el cronograma) en vez de forzar una fusión, para ser fieles a la fuente.
- **"Responsable" = "Equipo Leodega"** en todas las historias de los Sprint Backlogs. Ninguna historia tiene un integrante asignado individualmente en el documento fuente; en vez de inventar asignaciones, se usó un valor genérico. **Es fácilmente reemplazable** por nombres reales más adelante (es solo texto en la tabla).
- **Fechas de sprint = rangos de "Día N" relativos**, no fechas de calendario. El cronograma PERT/CPM original solo tiene días relativos (0 a 52), sin fechas reales. Se dividieron los 52 días en 5 sprints aproximadamente iguales (11/10/11/10/10 días). El instructivo del curso no exige fechas de calendario exactas, así que esto es válido; **también reemplazable** por fechas reales cuando se definan.
- **Estado de todas las historias = "Pendiente"**, no "Hecho". Corrección importante del usuario: el proyecto fue *empezado por un equipo anterior* (plataforma base ya entregada: auth, RBAC, publicación/moderación de almacenes, búsqueda, reservas, calificaciones, reportes, notificaciones, mensajería, pagos). El equipo actual **extiende** esa base (organizaciones multiempresa, Leodeguita, integración de inventario, cumplimiento de permiso de bomberos, panel de administrador), y en el primer parcial *solo se especificaron* los requerimientos — no se implementó nada todavía. Por eso todas las 38 historias del backlog están "Pendiente".
- **Cliente = "Leonardo"** (fallback explícito pedido por el usuario): es el único nombre de cliente que aparece en el documento fuente (actas de reunión), sin apellido.
- **Cambio de portada no revertido**: en algún momento el logo se agrandó a 14cm, se quitaron las líneas divisorias (`\rule`) alrededor del título "LEODEGA", y el header de la tabla de integrantes pasó de "Matrícula / ID" a "Matrícula". Esto se preservó tal cual (fue un cambio intencional, no un error a corregir).

---

## 4. Lo que yo (Claude) puedo y no puedo hacer desde aquí

**Sí puedo:**
- Redactar documentación de casos de uso, justificación de SOLID/patrones de diseño, y descripciones de diagramas de actividad/secuencia/estado — **si se me da el diagrama ya elaborado** (imagen o descripción) o una lista clara de entidades/flujos del sistema.
- Seguir ajustando formato LaTeX (tablas, estilos, estructura).
- Rellenar cualquier campo puntual (fechas, nombres, porcentajes) en cuanto se me proporcionen.

**No puedo:**
- Generar los diagramas UML en sí (casos de uso, clases, objetos, componentes, despliegue, actividad, secuencia, colaboración, estado) como los pide la rúbrica — estos deben modelarse en una herramienta como Visual Paradigm, StarUML o draw.io y exportarse como imagen a `imagenes/`.
- Inventar datos que no están en ninguna fuente (matrículas, % de aporte individual, fechas de calendario reales, nombre de la herramienta ágil usada) sin que el usuario los confirme.

---

## 5. Pendientes concretos (para retomar)

1. ~~Matrícula de los 4 integrantes~~ — resuelto.
2. ~~Actas de conformidad~~ — **resueltas ambas**. `imagenes/acta_conformidad_ers.pdf` (Anexo, ver §2 punto 34) y `imagenes/acta_conformidad.pdf` (cuerpo principal, ver §2 punto 35), ambas firmadas por Leonardo Castro (Product Owner, Leodega).
3. ~~Nombre de la herramienta ágil~~ — resuelto: Jira.
4. Sección B: completa. Casos de Uso, Clases (dividido en 10 diagramas, ver §2 punto 26), Objetos, Componentes y Despliegue ya están todos completos (ver §1).
5. Sección C: **completa**. ~~Actividad~~ (12 diagramas, pull `fe7cf29`, ver §2 punto 27), ~~Colaboración/Comunicación~~ (6 diagramas, ver §2 punto 29), Secuencia (32) y Estado (5) — todos completos.
6. ~~Contribuciones individuales (nombres y %)~~ — resuelto.
7. ~~Anexo (Sección de Anexos)~~ — **resuelto** (ver §2 punto 34): ERS mejorado completo (`secciones/anexo_ers.tex`), prototipo con flujo de ventanas y 34 capturas reales (`secciones/anexo_prototipo.tex`), y acta de conformidad del anexo.
8. ~~Gitlink huérfano `recursos/Leodega-codigo`~~ — resuelto (ver §2 punto 28): quitado del índice y agregado a `.gitignore`.
9. `git push` al repositorio remoto — sigue sin hacerse. Además, `recursos/informacion_general/proyecto1P.{tex,pdf}` (carpeta con guion bajo) aparecen borrados en `git status` — el usuario los reemplazó localmente por `recursos/informacion-general/` (con guion, nueva, sin trackear todavía). Ningún cambio de esta sesión ni de las anteriores se ha commiteado ni pusheado.
