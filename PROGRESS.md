# Seguimiento del proyecto — Especificación de Proyecto Final (Leodega)

> Documento de control interno (no forma parte del entregable). Registra qué se ha hecho, qué decisiones se tomaron y por qué, y qué falta, para poder retomar el trabajo sin perder contexto.

**Curso:** SOFG1007 — Ingeniería de Software I (ESPOL/FIEC)
**Proyecto real:** Leodega (marketplace de almacenamiento), continuación de un sistema entregado por un equipo anterior.
**Fuente de la plantilla:** `project_instructions.md`.

---

## 1. Estado por sección

| Sección | Estado | Notas |
|---|---|---|
| Portada | 🟡 Casi completa | Falta la matrícula (`[ID]`) de los 4 integrantes |
| Acta de Conformidad del Cliente | 🔴 Pendiente | Requiere el PDF firmado real; penalidad de -100 si falta |
| Índices (TOC/LOF/LOT) | 🟢 Automáticos | No requieren edición manual |
| Información General del Proyecto | 🟢 Completa | Descripción, objetivos y alcance, tomados de `proyecto1P.tex` |
| Sección A — Gestión de Riesgos | 🟢 Completa | Matriz 5×5 + 9 riesgos evaluados y con plan de respuesta |
| Sección A — Sprint Backlogs | 🟢 Completa | 5 sprints, 38 historias reales | Ver convenciones (§3) sobre "Responsable" y fechas |
| Sección A — Cronograma | 🟢 Completa | Tabla CPM, ruta crítica, AOA y recursos limitados (imágenes reales del usuario) + Gantt generado por Claude |
| Sección B — Casos de uso | 🟢 Completa | 6 diagramas PlantUML reales (1 general de contexto + 5 por subsistema), tabla de actores (8 actores), catálogo consolidado de 43 UC y 11 fichas extendidas de los casos medulares |
| Sección B — Diagrama de clases (SOLID/patrones/malos olores) | 🟢 Completa | PlantUML real (19 modelos + 6 servicios + 2 policies + enum + 2 excepciones + 6 clases nuevas planificadas), tablas de SOLID, patrones y code smells, tabla de trazabilidad |
| Sección B — Objetos | 🟢 Completa | 2 diagramas de objetos reales (reserva confirmada con pago; reporte de incidente), valores coherentes con el diagrama de clases |
| Sección B — Componentes y Despliegue | 🟢 Completa | PlantUML real (recursos/diagramas_uml/), sistema completo heredado+nuevo marcado 🟦/🟩, con tabla de trazabilidad |
| Sección C — Actividad/Secuencia (32 mín.)/Colaboración | 🔴 Pendiente | Placeholders |
| Sección C — Estado | 🟢 Completa | 5 diagramas reales (`Reservations`, `StoreRooms`, `Reports`, `Payments`, `User`), cada uno con tabla de trazabilidad; documentan además 2 hallazgos reales de inconsistencia en el código (ver §2, punto 14) |
| Contribuciones Individuales | 🔴 Pendiente | Nombres y % de aporte reales |
| Referencias | 🟢 Completa | Sin placeholders |
| Anexos | 🟡 Parcial | Estructura lista, contenido específico pendiente |

Leyenda: 🟢 completo · 🟡 parcial/falta un dato puntual · 🔴 pendiente (trabajo sustancial)

---

## 2. Qué se hizo, en orden

1. **Plantilla LaTeX inicial** generada a partir de `project_instructions.md`: estructura completa (`main.tex` + `secciones/*.tex`), con placeholders en todo lo que es específico del proyecto. Logo ESPOL (no ETS). Corregidos dos bugs de compilación (guion bajo fuera de modo matemático → paquete `underscore`; `\\` seguido de `[` interpretado como argumento óptico → se separó con `\\{}`).
2. **Información General** (Descripción general, Objetivos, Alcance) completada con datos reales de `recursos/informacion_general/proyecto1P.tex` (el ERS del primer parcial).
3. **Gestión de Riesgos** completada con `recursos/riesgos/analisis_de_riesgo.md` (metodología, matriz 5×5, 6 estrategias) y `Estimación de Esfuerzo con COSMIC - Leodega.xlsx` (para justificar R-01/R-02 por concentración de tamaño funcional). 9 riesgos identificados y evaluados (R-01 a R-09), cada uno con estrategia, acciones de mitigación, plan de contingencia y responsable.
4. **Placeholders de portada** llenados: logo real (`imagenes/logo-espol.jpg`), nombre del sistema (Leodega), integrantes del equipo, profesor, cliente ("Leonardo", único nombre disponible en las actas de reunión) y período académico (2026 PAO2).
5. **Preparación del repositorio git** para `https://github.com/Darwin4050E/DocumentacionLeodega.git`: `git init`, `.gitignore` (excluye artefactos de compilación LaTeX y `.claude/settings.local.json`), commit `7d6684c` sobre el `README.md` ya existente en el remoto. **Aún no se ha hecho `git push`** (pendiente de confirmación del usuario, y hay cambios nuevos sin commitear desde entonces — ver `git status`).
6. **Corrección de tablas desbordadas**: la matriz de riesgo (Tabla 2.1) se ajustó con `\resizebox`; la tabla de plan de respuesta (Tabla 2.3) tenía el header "Responsable" desbordando su celda, se corrigió ensanchando la columna y reduciendo `\tabcolsep`.
7. **Sprint Backlogs completados** (5 sprints, 38 historias de usuario reales HUG/HUC/HUA/HUE/HUL, tomadas del capítulo "User Stories" de `proyecto1P.tex`, agrupadas por el campo "Sprint" que ya traía cada historia). Ver convenciones adoptadas en §3.
8. **Centrado de tablas**: se añadieron los tipos de columna reutilizables `M{ancho}` (centrado horizontal+vertical) y `L{ancho}` (alineado a la izquierda, centrado vertical) en `main.tex`, aplicados a **todas** las tablas del documento (Sección A, Sección B, portada, contribuciones) para que headers y contenido dejen de quedar pegados arriba.
9. **Cronograma del Proyecto completado**: tabla de 38 actividades (HST-001 a HST-038) con dependencias, duración, personal, EET/holgura/LET y marca de ruta crítica; texto de ruta crítica (52 días); sección nueva de planificación con recursos limitados (87→85 días, límite 4 personas). Todo tomado del capítulo "Project Planning and Control" de `proyecto1P.tex`.
10. **Imágenes del cronograma insertadas**: el usuario proporcionó las imágenes reales de AOA (`imagenes/activity-on-arrow.png`) y de recursos limitados sin optimizar/optimizado (`imagenes/resoruce-limited-without-optimizatio.png` y `imagenes/resource-limited-optimization.png` — nombres de archivo tal cual los dejó el usuario, con una errata en el primero que no se corrigió porque hay que respetar el nombre real del archivo en disco). No tenía el diagrama de Gantt, así que Claude lo generó programáticamente con Python/Matplotlib (`imagenes/diagrama_gantt.png`) a partir de los mismos datos EET/duración/ruta crítica de la tabla CPM, con la misma paleta de colores por sprint que las otras imágenes para mantener consistencia visual. Se documentó en el propio LaTeX que la herramienta utilizada fue "Python/Matplotlib" en vez de dejar el placeholder de herramienta comercial.
11. **Diagramas de Componentes y Despliegue generados** (primeros diagramas UML reales del entregable, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`): se exploró el código de `recursos/leodega_codigo/` (capas Controller→Service→Model, middleware, config de filesystems/database/CORS/sanctum) y la infraestructura real de despliegue (`railway.json`, `Procfile`, `DockerFIle`, `vercel.json`, `docker-compose.yml`, `trustHosts` en `bootstrap/app.php`), y se cruzó contra el ERS (`proyecto1P.tex`, sección "Project Context"/"Project Scope") para separar heredado 🟦 de nuevo 🟩. Confirmado por grep que ninguno de los requerimientos nuevos (organizaciones, Leodeguita, integración de inventario, permiso de bomberos, panel admin/métricas, moderación de cuentas con auditoría) tiene código todavía — solo existe el campo `User.state` como base parcial de moderación; por eso esos módulos se marcan "Nuevo (planificado)" con la sección del ERS como fuente, tal como indica la sección 4 de `INSTRUCCIONES_DIAGRAMAS.md` para elementos nuevos sin código. Fuentes PlantUML en `recursos/diagramas_uml/*.puml`, renderizadas a PNG localmente hacia `imagenes/diagrama_componentes.png` y `imagenes/diagrama_despliegue.png`. Se evitó el placeholder genérico de herramienta del macro `\DiagramaPagina` (igual que se hizo con el Gantt): estas dos secciones usan bloques manuales en `seccionB.tex` con la herramienta real ("PlantUML") y la tabla de trazabilidad obligatoria. Se agregaron los colores `heredadobg`/`nuevobg` en `main.tex` (mismos tonos que el `skinparam` de los `.puml`) para colorear la columna "Estado" de las tablas. Se compiló el documento completo sin errores; se corrigieron overfull hbox usando `\url{}` en vez de `\texttt{}` para rutas de archivo largas (permite salto de línea en `/`) y reestructurando la oración del despliegue que encadenaba demasiados tokens verbatim seguidos.
12. **Actualización de herramienta PlantUML (2026-08-12)**: el usuario instaló PlantUML v1.2026.6 en `~/.local/share/plantuml/plantuml.jar` junto con Graphviz (`dot` 2.43.0), que antes faltaba y había forzado el uso del motor de layout alternativo Smetana (más tosco). Se quitó el `!pragma layout smetana` de ambos `.puml`, se regeneraron los PNG con el motor nativo `dot` (notoriamente más limpio: usa los íconos UML de componente correctos y mejor distribución) y se actualizó el texto "Herramienta de modelado utilizada" en `seccionB.tex` para citar la versión real y el motor Graphviz/`dot`. Recompilado sin errores nuevos.
13. **Diagrama de Clases, Objetos y Estado generados (2026-08-13)**, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`: se exploró a fondo `recursos/leodega_codigo/backend` (19 modelos Eloquent, 6 servicios de aplicación, 2 policies, 1 enum, 2 excepciones de dominio, y las migraciones para extraer atributos/tipos/enums exactos y las reglas `onDelete(cascade)` que fundamentan cada composición vs. asociación del diagrama de clases). El **Diagrama de Clases** (`recursos/diagramas_uml/diagrama_clases.puml`) modela toda la capa de lógica de negocio heredada más 6 clases nuevas planificadas (`Organizacion`, `OrganizacionMiembro`, `ConexionInventario`, `ProductoInventario`, `PermisoBomberos`, `RegistroAuditoria`) extraídas del ERS (`proyecto1P.tex`, HUE-01..06, HUC-10..13, HUA-01..03); se documenta además el patrón real de "perfil compuesto" (`User` 1–(0..1) `Landlords`/`Tenants`/`Admin` vía `hasOne`, no herencia). Se llenaron las tablas de SOLID y Patrones de Diseño que ya traía la plantilla (Service Layer, Template Method en `ApiController`, Static Factory en `NotificationService::send()`, Strategy en las Policies, Fail-Fast con excepciones de dominio) y se agregó una tabla nueva de **malos olores** (obsesión por primitivos en los 6 campos de estado tipo string, ausencia de interfaces PHP, ausencia de máquina de estados explícita), no prevista originalmente en la plantilla pero exigida por la rúbrica (`project_instructions.md`, fila "Sección B" de la rúbrica incluye "SOLID, patrones de diseño, malos olores"). Los **Diagramas de Objetos** (`objetos_01.puml`, `objetos_02.puml`) instancian dos escenarios reales con IDs e valores coherentes entre sí: reserva confirmada con pago (`ReservationService::confirm()`) y reporte de incidente con notificación en cascada a admins+landlord (`ReportService::create()`). El **Diagrama de Estado** (`estado_01.puml`) documenta el ciclo de vida completo de `Reservations.status` (pending/confirmed/canceled, extraído de `ReservationService` y de la migración de la tabla), incluyendo la cancelación automática en cascada y el guard de conflicto de fechas; se agregó un estado "Finalizada" marcado explícitamente como nuevo/planificado (sugerido por HUG-08 del ERS, sin job en el código actual) para no inventar un estado inexistente sin dejarlo trazado. **Bug de compilación encontrado y corregido**: los caracteres `"` (comillas rectas) usadas en la prosa nueva colisionaban con los shorthands activos de `babel[spanish]` (p. ej. `"u` → ü), corrompiendo el texto compilado; se reemplazaron por las comillas tipográficas `` `` / `''` ya usadas en el resto del documento (y se corrigieron dos casos preexistentes en la plantilla que tenían el mismo problema, `seccionB.tex` línea 67 y `seccionC.tex` línea 24). Se evitó el placeholder genérico del macro `\DiagramaPagina` para objetos y estado (mismo criterio que en el punto 11), usando bloques manuales con la herramienta real y tabla de trazabilidad. Compilado sin errores ni overfull/underfull hbox (105 páginas).
14. **Diagramas de Estado restantes completados (2026-08-13)**, a pedido explícito del usuario ("completa los diagramas de estado faltantes"). Se identificaron 4 clases adicionales con comportamiento dependiente de estado (ya señaladas como candidatas en el punto 13 y en la tabla de Malos Olores de `seccionB.tex`): `StoreRooms.publication_status`, `Reports.status`, `Payments.payment_state` y `User.state`. Para cada una se exploró el controlador y el Form Request reales (`StoreRoomsController`, `StoreModerationController`, `ReportsController`, `PaymentsController`, `UserController`, `AdminController`, `SecurityController`, `AuthController`) antes de modelar, siguiendo el mismo criterio de fidelidad que en el punto 13. La exploración destapó **dos hallazgos reales de inconsistencia en el código**, documentados explícitamente en los diagramas (no solo mencionados, sino modelados como estados/transiciones marcados en rojo):
    - `Reports.status`: la migración declara `enum('status', ['pending','in_review','resolved'])`, pero `UpdateReportStatusRequest::rules()` valida `'status' => 'in:resolved,canceled'` y `ReportsController::updateStatus()` efectivamente escribe el string `"canceled"`, que **no pertenece al enum de la base de datos** (⚠️ corrección ver punto 16: se verificó empíricamente que esto falla igual en SQLite y en PostgreSQL — no es un problema exclusivo de producción). Además, `in_review` es un valor del enum que ningún endpoint del código actual asigna (estado muerto).
    - `User.state`: `AuthController::login()` solo llama a `Auth::attempt(email, password)` — no existe ningún middleware/guard que verifique `state`, por lo que una cuenta `pending` o `blocked` puede iniciar sesión con éxito hoy. Se documentó también que `AuthController::register()` no asigna `state` explícitamente (nace en `pending` por el `default` de la migración `create_user_table`, la tabla `user` real que referencian las FKs — no la tabla `users` muerta del scaffold).
    También se documentó que `StoreRooms.publication_status` y `StoreModeration.status` son dos fuentes de verdad **independientes** (mismo enum, sin sincronización en el código), y que `PaymentsController` **nunca invoca** `ReservationService::confirm()` (registrar un pago y confirmar la reserva son dos operaciones HTTP desacopladas, sin transacción del servidor que las una). Se agregó el color `gapbg` (`\definecolor{gapbg}{RGB}{250,219,216}`) en `main.tex` para la columna "Estado" de las tablas de trazabilidad, reservado específicamente para marcar inconsistencias detectadas (distinto de "Heredado"/"Nuevo"). Se corrigió además, de paso, el mismo bug de comillas rectas vs. shorthands de `babel[spanish]` (ver punto 13) encontrado en `secciones/contribuciones.tex` (preexistente en la plantilla, no introducido en este parcial). Compilado sin errores ni overfull/underfull hbox (113 páginas).
15. **`PLAN_CORRECCION_INCONSISTENCIAS.md` creado (2026-08-13/14)**, a pedido del usuario, como documento de diseño separado de la documentación LaTeX. Cubre plan de corrección para los 5 hallazgos del punto 14, en 3 fases. Tras una aclaración del usuario ("no podemos implementar la Fase 3 todavía, es parte de un sprint"), se verificó en `secciones/seccionA.tex` que HUA-03 (bloqueo/reactivación de cuentas) ya está en el Sprint Backlog (Sprint 1, 5 puntos, "Pendiente"), y se retiró la Fase 3 del plan de correcciones para no planificar la misma historia en dos documentos desincronizados — quedó solo como nota de referencia. El guard de login de la Fase 1.2 se acotó a `state === 'blocked'` explícitamente, sin tocar el significado de `pending`, para no adelantar decisiones de diseño de esa historia.
16. **Fases 1 y 2 del plan implementadas en `recursos/leodega_codigo/backend` (2026-08-14)**, a pedido explícito del usuario ("ejecuta el plan... comprueba que TODO el código funcione"). Se instaló el entorno (`composer install`, `npm install`), se estableció un baseline limpio (98/109 tests preexistentes — el resto son los agregados en este pase) y se corrigieron los 4 hallazgos de heredado (no se tocó el #5-B/HUA-03, confirmado fuera de alcance en el punto 15): (1) migración de `Reports.status` con SQL específico por motor (`Schema::getConnection()->getDriverName()`), verificada contra un PostgreSQL 16 real vía Docker desechable — se descubrió ahí que `$table->enum(...)->change()` de Laravel 12 genera SQL inválido en PostgreSQL (funciona en SQLite), y que el bug original fallaba en **ambos** motores, no solo en producción (corrección al punto 14 de este mismo archivo); (2) guard de login para `state === 'blocked'` (`AccountBlockedException`); (3) `StoreModerationService` sincronizando `StoreRooms.publication_status` con `StoreModeration`, integrado de forma no invasiva dentro de `StoreRoomsController::update()` (en vez de deprecar ese endpoint, como sugería el plan original, para no romper el contrato ya usado por tests/frontend) y también en `StoreModerationController::store()`; (4) `PaymentService` conectando `Payments` con `ReservationService::confirm()` dentro de una transacción, más `PaymentsPolicy` de ownership (hallazgo adicional: `/payments` no validaba que el pagador fuera el tenant dueño de la reserva). Verificación completa antes de dar por bueno: PHPUnit 109/109 (11 tests nuevos), PHPStan 0 errores (2 falsos positivos de propiedades dinámicas de Eloquent añadidos al baseline, mismo criterio que las 33 entradas ya existentes), PHPMD 0 violaciones, Pint 124 archivos conformes, Vitest del frontend 33/33 (sin cambios ahí). Se confirmó por `grep` que el frontend no llama a `/payments` todavía (cero riesgo de romper algo) y que `/admin/bodegas` (única UI que aprueba bodegas) ya está gateada a `role: admin` en `Routes/Role.tsx`, consistente con el nuevo chequeo de rol agregado en el backend. **Nada se commiteó ni se pusheó** — el usuario prepara y sube los commits sobre el working tree del repo `LeodegaPruebas`. Detalle completo de decisiones y desviaciones del plan original en `PLAN_CORRECCION_INCONSISTENCIAS.md`, §3–§10. *(Actualización: los commits se prepararon en el punto 17 — ver ahí.)*
17. **Commits preparados en `LeodegaPruebas` (2026-08-14)**: 6 commits (`Fix: la suite de tests no corría por falta de APP_KEY...`, `Fix: Reports.status aceptaba "canceled"...`, `Fix: login no validaba cuentas bloqueadas...`, `Fix: StoreRooms y StoreModeration quedaban desincronizados...`, `Fix: Payments no confirmaba la reserva...`, `Documentación: registra las correcciones...`), siguiendo el estilo ya establecido en el repo (prefijo `Fix:`/`Documentación:`, doc como commit final, sin `Co-Authored-By`). De paso se detectó que `PLAN_MAESTRO_REFACTORIZACION.md` (bitácora de la refactorización anterior, restaurado por el usuario tras aparecer borrado del working tree) necesitaba una entrada nueva siguiendo su propio formato de "Post-verificación", ya que documenta con el mismo rigor cada cambio sustantivo al código — se agregó, dejando explícito que este trabajo se originó fuera de ese plan (en `ingeParcial2/`, no en una fase de la refactorización). Working tree limpio y 109/109 tests confirmados después de separar todo en commits. **No se hizo push** — queda a cargo del usuario.
18. **Diagramas de Sección B/C rehechos tras los fixes del punto 16 (2026-08-14)**, a pedido explícito del usuario. Se identificaron 6 diagramas afectados: el **Diagrama de Clases** (faltaban `StoreModerationService`, `PaymentService`, `PaymentsPolicy`, `AccountBlockedException`, 2 casos nuevos de `NotificationType`, y el atributo `Reports.status` seguía mostrando el enum viejo) y los **5 Diagramas de Estado** (`Reservations`: nuevo disparador de `confirm()` desde `PaymentService`; `StoreRooms` y `Reports`: los hallazgos centrales que documentaban ya estaban corregidos, requirieron reescritura sustancial; `Payments` y `User`: igual, el gap que señalaban en rojo/verde-planificado ahora es heredado real). **Bug de PlantUML encontrado y corregido**: una nota del diagrama de estado de `Reports` perdía fragmentos enteros de texto sin error visible — la causa es que PlantUML trata cualquier línea que empiece con `'` (comilla simple), incluso dentro de un bloque `note`, como comentario y la descarta silenciosamente; se verificó por `grep` que ningún otro `.puml` del proyecto (incluidos los ya entregados) tenía el mismo patrón. Se evitó reintroducir comillas simples al inicio de línea en vez de intentar escapar el carácter. Las 6 imágenes se copiaron a `imagenes/`, se actualizaron las descripciones y tablas de trazabilidad de Secciones B y C (incluyendo las tablas de Patrones de Diseño y Malos Olores de \S3.2 para mencionar las 4 clases nuevas), y se recompiló sin errores ni overfull/underfull hbox (113 páginas, mismo conteo que antes).
19. **Ajuste de estilo en las descripciones (2026-08-14)**: el usuario pidió que las descripciones de los diagramas no narren "esto se corrigió"/"antes X, ahora Y" con fecha, sino que describan el sistema actual directamente. Se reescribieron los párrafos de "Descripción" de las Secciones B y C (quitando encabezados tipo "Actualización (2026-08-14):"/"Corregido (2026-08-14):" y fusionando el contenido en prosa continua) y también las notas dentro de los propios `.puml` (que tenían la misma redacción, visible en las imágenes). Las tablas de trazabilidad conservan celdas "Pendiente"/"Nuevo (planificado)" para gaps que siguen existiendo hoy — eso no es narrativa de corrección, es estado actual del sistema. Recompilado sin errores, mismo conteo de páginas (113).
20. **Bug de layout corregido en Sección B (2026-08-14)**: el usuario reportó una hoja en blanco después de la tabla de trazabilidad del diagrama de clases, seguida de la tabla SOLID desbordando el margen inferior, seguida de otra hoja en blanco. Causa raíz: las tablas de SOLID, Patrones de Diseño y Malos Olores usaban `\begin{table}[H]...\begin{tabular}` (float no paginable del paquete `float`), mientras que todas las demás tablas del documento (incluidas las de trazabilidad) ya usaban `longtable`. Al crecer el contenido de esas tres tablas (nuevas clases/patrones agregados en los puntos 18-19), la tabla SOLID completa ya no cabía en una sola página; al ser un `tabular` no divisible, LaTeX empujó el bloque completo (encabezado + tabla) a la página siguiente (dejando la anterior en blanco) y aun así se desbordó, por ser más alta que una página. Se convirtieron las tres tablas a `longtable` (mismo patrón `\endfirsthead`/`\endhead`/`\endfoot`/`\endlastfoot` con "(continuación)" que ya usan las tablas de riesgos y sprint backlogs de la Sección A), lo que les permite partirse naturalmente entre páginas. Verificado visualmente: sin hojas en blanco, sin desbordes, `\caption`/`\label` (Tabla 3.4/3.5/3.6) preservados y sus referencias cruzadas (`\ref{tab:solid}`, etc.) intactas. Recompilado sin errores, mismo conteo de páginas (113).
21. **Diagramas de Casos de Uso completados (2026-08-14)**, siguiendo `INSTRUCCIONES_DIAGRAMAS.md`. Se exploró el código real como fuente de verdad de lo soportado (`backend/routes/api.php`, controladores, servicios `ReservationService`/`PaymentService`/`ReportService`/`StoreModerationService`, enum `NotificationType`, `AuthController::login` con `AccountBlockedException`) y se cruzó contra el ERS (`proyecto1P.tex`, HUG/HUC/HUA/HUE/HUL) para separar heredado 🟦 de nuevo 🟩. **Actores identificados (8):** Visitante, Usuario Registrado (abstracto, generaliza a los 3 roles), Cliente, Arrendador, Administrador, Administrador de Organización (nuevo, especializa a Cliente), y dos secundarios/externos: Servicio de Correo (heredado, `ResetPasswordNotification`) y Sistema de Inventario Externo (nuevo). Decisión fundamentada: el pago es simulado (ERS HUC-05, fuera de alcance) → **no** se modela pasarela como actor externo; Leodeguita (HUL) se documenta como canal móvil que reutiliza los mismos UC, no como UC duplicados. **Decisión de diseño:** en vez de un único diagrama con ~43 UC (ilegible, la rúbrica penaliza hasta -50 por legibilidad), se hicieron **6 diagramas**: 1 general de contexto (`diagrama_casos_uso.puml`, actores+jerarquía+9 subsistemas) + 5 detallados por subsistema (`casos_uso_01..05.puml`: Autenticación/Cuenta/Comunicación; Cliente; Arrendador; Administrador; Módulos nuevos Organizaciones+Inventario). En `seccionB.tex` se reemplazó el placeholder por: intro, tabla de actores, los 6 diagramas (cada uno con descripción, herramienta PlantUML y tabla de trazabilidad, patrón manual igual a Componentes/Objetos), un **catálogo consolidado de 43 UC** (longtable `footnotesize`) y **11 fichas extendidas** (formato `ref21`) de los UC transaccionales medulares (Registrarse, Login, Reservar, Confirmar pago, Confirmar reserva landlord, Publicar bodega, Reportar incidente, Moderar publicación, Bloquear/Reactivar cuenta, Invitar a organización, Ver inventario sincronizado). **Dos bugs encontrados y corregidos:** (1) en `babel[spanish]` los caracteres `<` y `>` son *shorthands activos* (generan guillemets «»); escribir `{<}{<}include{>}{>}` rompe la compilación con "Argument of \\language@active@arg< has an extra }" — se usó `\guillemotleft include\guillemotright` (notación UML correcta). Misma familia que el bug de comillas de los puntos 13/14. (2) Overfull hbox por `\texttt{Clase::metodo/metodo}` largos que no cortan en columnas angostas → se convirtieron a `\url{}` (corta en `/` y `:`, mismo criterio que las rutas ya existentes) y se envolvió la sección en `\sloppy`/`\fussy`. **Entorno:** esta máquina no tenía `dot`/Graphviz ni toolchain LaTeX (a diferencia de sesiones previas); se descargó `plantuml.jar` v1.2026.6 a `~/.local/share/plantuml/` y se renderizó con el flag CLI `-Playout=smetana` (motor Java puro, sin tocar los `.puml` para mantenerlos consistentes con los otros diagramas que usan `dot`); se compiló con el binario portable `tectonic` 0.17.0. Verificado: compila sin errores (exit 0), **135 páginas**, sección nueva sin overfull significativo (máx 9pt), diagramas legibles confirmados visualmente sobre el PDF. `main.pdf` actualizado.
22. **Ajustes de casos de uso a pedido del usuario (2026-08-14)**: (a) el usuario editó los 6 `.puml` quitando los `legend` y las `note` explicativas, y el "(simulado)" de "Confirmar pago"; se re-renderizaron los 6 PNG con Smetana y se alineó `seccionB.tex` (etiqueta del UC "Confirmar pago" en descripción/catálogo/ficha; se conservó "pago simulado (HUC-05)" solo como hecho de negocio en la prosa). (b) Se rehízo el **formato de las 11 fichas** al pedido por el usuario: rejilla de 4 columnas con bordes completos, etiqueta en negrita/derecha/fondo gris, filas 2-3 con dos pares etiqueta-valor (Creado por | Actualizado por; Fecha de creación | Última revisión), listas numeradas en pre/post/flujo/alternativos (o "—" si vacío), requerimientos en viñetas de guión, y pie en cursiva centrado "Tabla N. \<nombre\>.". Se creó un **entorno reutilizable `casouso`** en el preámbulo de `main.tex` (columntype `K` gris/negrita/derecha, `\cuvalwidth` por `\dimexpr` para el span de 3 columnas, y macros `\CUnombre`/`\CUmeta`/`\CUfechas`/`\CUdescripcion`/`\CUactores`/`\CUprecondiciones`/`\CUpostcondiciones`/`\CUflujo`/`\CUalternativos`/`\CUexcepciones`/`\CUrequerimientos`) para que cada ficha solo cambie contenido. Los campos "Disparador"/"Reglas de Negocio"/"Supuestos" del formato anterior se refundieron en Descripción/Flujo/Requerimientos. Metadatos: autores = "Equipo Leodega", fechas = 14/08/2026 (fecha real de redacción, reemplazable). **Gotcha de numeración:** en este entorno todo `longtable` incrementa el contador `table` aunque no tenga `\caption` (por eso las tablas de trazabilidad ya "comían" números y el catálogo quedó en Tabla 3.7); el `\refstepcounter{table}` puesto en el entorno hacía contar doble (3.9/3.11/3.13…). Se quitó y cada ficha usa el número que su propio `longtable` avanza → consecutivo 3.8–3.18. Verificado: exit 0, **138 páginas**, fichas sin overfull, formato confirmado visualmente.

---

## 3. Decisiones y convenciones adoptadas (el "por qué")

- **Los IDs de historia (HUG/HUC/HUA/HUE/HUL) y los IDs de actividad (HST-XXX) son intencionalmente independientes.** `proyecto1P.tex` los usa como dos numeraciones distintas: HU-XX para las historias de usuario (Sección de Requerimientos) y HST-XXX para las actividades del análisis PERT/CPM. Se verificó que hay correspondencia 1 a 1 entre ambos conjuntos (38 = 38, mismas duraciones/sprint), pero se documentan por separado (con nota al pie en el cronograma) en vez de forzar una fusión, para ser fieles a la fuente.
- **"Responsable" = "Equipo Leodega"** en todas las historias de los Sprint Backlogs. Ninguna historia tiene un integrante asignado individualmente en el documento fuente; en vez de inventar asignaciones, se usó un valor genérico. **Es fácilmente reemplazable** por nombres reales más adelante (es solo texto en la tabla).
- **Fechas de sprint = rangos de "Día N" relativos**, no fechas de calendario. El cronograma PERT/CPM original solo tiene días relativos (0 a 52), sin fechas reales. Se dividieron los 52 días en 5 sprints aproximadamente iguales (11/10/11/10/10 días). El instructivo del curso no exige fechas de calendario exactas, así que esto es válido; **también reemplazable** por fechas reales cuando se definan.
- **Estado de todas las historias = "Pendiente"**, no "Hecho". Corrección importante del usuario: el proyecto fue *empezado por un equipo anterior* (plataforma base ya entregada: auth, RBAC, publicación/moderación de almacenes, búsqueda, reservas, calificaciones, reportes, notificaciones, mensajería, pagos). El equipo actual **extiende** esa base (organizaciones multiempresa, Leodeguita, integración de inventario, cumplimiento de permiso de bomberos, panel de administrador), y en el primer parcial *solo se especificaron* los requerimientos — no se implementó nada todavía. Por eso todas las 38 historias del backlog están "Pendiente".
- **Cliente = "Leonardo"** (fallback explícito pedido por el usuario): es el único nombre de cliente que aparece en el documento fuente (actas de reunión), sin apellido.
- **Cambio de portada no revertido**: en algún momento el logo se agrandó a 14cm, se quitaron las líneas divisorias (`\rule`) alrededor del título "LEODEGA", y el header de la tabla de integrantes pasó de "Matrícula / ID" a "Matrícula". Esto se preservó tal cual (fue un cambio intencional, no un error a corregir).

---

## 4. Lo que yo (Claude) puedo y no puedo hacer desde aquí

**Sí puedo:**
- Redactar documentación de casos de uso, justificación de SOLID/patrones de diseño, y descripciones de diagramas de actividad/secuencia/estado — **si se me da el diagrama ya elaborado** (imagen o descripción) o una lista clara de entidades/flujos del sistema.
- Seguir ajustando formato LaTeX (tablas, estilos, estructura).
- Rellenar cualquier campo puntual (fechas, nombres, porcentajes) en cuanto se me proporcionen.

**No puedo:**
- Generar los diagramas UML en sí (casos de uso, clases, objetos, componentes, despliegue, actividad, secuencia, colaboración, estado) como los pide la rúbrica — estos deben modelarse en una herramienta como Visual Paradigm, StarUML o draw.io y exportarse como imagen a `imagenes/`.
- Inventar datos que no están en ninguna fuente (matrículas, % de aporte individual, fechas de calendario reales, nombre de la herramienta ágil usada) sin que el usuario los confirme.

---

## 5. Pendientes concretos (para retomar)

1. Matrícula de los 4 integrantes (portada).
2. PDF del acta de conformidad firmada por el cliente.
3. Nombre de la herramienta ágil (Jira/Trello/Azure DevOps/etc.) para los Sprint Backlogs (el Gantt ya quedó documentado como generado con Python/Matplotlib).
4. Sección B: completa. Casos de Uso, Clases, Objetos, Componentes y Despliegue ya están todos completos (ver §1).
5. Sección C: falta actividad, ≥32 secuencia y colaboración. Estado está completo (5 diagramas: Reservations, StoreRooms, Reports, Payments, User).
6. Contribuciones individuales (nombres y %).
7. `git push` al repositorio remoto — preparado pero no ejecutado; hay cambios sin commitear (`main.tex`, `secciones/*.tex`, `main.pdf`, y las 4 imágenes nuevas en `imagenes/`).
